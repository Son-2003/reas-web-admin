export enum TypeCriticalReport {
  RESIDENT = 'RESIDENT',
  FEEDBACK = 'FEEDBACK',
  EXCHANGE = 'EXCHANGE',
}

export enum StatusCriticalReport {
  PENDING = 'PENDING',
  RESOLVED = 'RESOLVED',
  REJECTED = 'REJECTED',
}
